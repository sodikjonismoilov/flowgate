<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.flowgate</groupId>
        <artifactId>flowgate</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>flowgate-service</artifactId>
    <name>flowgate-service</name>
    <description>
        Standalone Spring Boot service demonstrating flowgate as a multi-tenant
        AI API gateway. POST /v1/completions is protected by @RateLimit, enforcing
        a distinct Redis-backed quota per tenant API key.
    </description>

    <dependencies>
        <!-- Core algorithm implementations — the actual rate limiting logic -->
        <dependency>
            <groupId>com.flowgate</groupId>
            <artifactId>flowgate-core</artifactId>
        </dependency>

        <!--
            The @RateLimit annotation, RateLimitAspect, and FlowgateAutoConfiguration.
            Adding this JAR is the entire integration story: Spring Boot auto-discovers
            FlowgateAutoConfiguration via META-INF/spring/...AutoConfiguration.imports,
            registers the RedisClient and RateLimitAspect beans, and any @RateLimit-annotated
            controller method in this module is enforced automatically.
        -->
        <dependency>
            <groupId>com.flowgate</groupId>
            <artifactId>flowgate-library</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!--
            Structured (JSON) logging. Spring Boot already puts Logback on the
            classpath via spring-boot-starter-logging; this adds the encoder that
            renders each event as a single JSON object instead of a text line.

            Why it matters here: CorrelationIdFilter / CorrelationIdGrpcInterceptor
            put the correlation ID into SLF4J's MDC. LogstashEncoder serialises the
            whole MDC map as top-level JSON fields, so `correlationId` becomes a
            queryable field in Loki/ELK/CloudWatch rather than text buried in a
            message. With the default text encoder the MDC would be silently dropped
            unless every pattern string remembered to reference %X{correlationId}.

            Not managed by the Spring Boot BOM, so the version is explicit.
            7.4 is the release line built against Logback 1.5.x, which is what
            spring-boot-starter-parent 3.3.0 resolves.
        -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>7.4</version>
        </dependency>

        <!-- Embedded Tomcat + Spring MVC for the POST /v1/completions endpoint -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- /actuator/health, /actuator/info endpoints -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>net.devh</groupId>
            <artifactId>grpc-server-spring-boot-starter</artifactId>
            <version>3.1.0.RELEASE</version>
        </dependency>

        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-stub</artifactId>
            <version>1.68.1</version>
        </dependency>


        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
            <version>1.3.2</version>
        </dependency>

        <!--
            TODO (Week 5): JWT auth for the service itself
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
            </dependency>
        -->

        <!-- ─── Test ──────────────────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>



    <build>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>1.7.1</version>
            </extension>
        </extensions>
        <plugins>
            <!--
                spring-boot-starter-parent only *manages* this plugin — it still has to be
                declared to run. Without it `mvn package` produces a plain library jar with
                no Main-Class, and `java -jar service/target/flowgate-service-*.jar` fails
                with "no main manifest attribute". Declaring it binds the `repackage` goal,
                producing an executable fat jar — which is also what a Dockerfile needs.
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.xolstice.maven.plugins</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>
                <version>0.6.1</version>
                <configuration>
                    <protocArtifact>com.google.protobuf:protoc:3.25.5:exe:${os.detected.classifier}</protocArtifact>
                    <pluginId>grpc-java</pluginId>
                    <pluginArtifact>io.grpc:protoc-gen-grpc-java:1.68.1:exe:${os.detected.classifier}</pluginArtifact>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <goal>compile-custom</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>